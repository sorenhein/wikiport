* Start on the write part
  - Make statistics to start with, don't write yet
  - AmVac csv 40.000.000 -- is it only that column or general problem?

* csv skip first 3 bytes
* skip inputs with new org or deal

* Can we fix "Deal decided"?
* Make Country field?
* How to deal with new orgs (not in Affinity)?


When the content-type of the server is 'Content-Type:text/html', requests.get() returns improperly encoded data.

However, if we have the content type explicitly as 'Content-Type:text/html; charset=utf-8', it returns properly encoded data.
